{
  "name": "matchstick-coverage-issue-reproduction",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "test": "graph test -d",
    "coverage": "graph test -dc"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.69.0",
    "@graphprotocol/graph-ts": "^0.34.0",
    "matchstick-as": "^0.6.0"
  }
}
